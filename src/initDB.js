import { checkExistFile } from "./helpers/checkExistFile.js";

/**
 * @param {string} filePath path to DB
 * @param {string} codeForInit SQL for init your DB
 * @return {db} return DB object
 */
export default async function (filePath, codeForInit) {
  if (!checkExistFile) {
  }
}
